import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    background: {
      backWhite: "#FFF",
    },
    text: {
      main: "#1f3251",
      pColor: "#7b879d",
    },
  },
  typography: {
    fontFamily: "Outfit",
  },
});

export default theme;
